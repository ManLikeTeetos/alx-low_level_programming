#include "main.h"
#include <string.h>

/**
 *_strcat - concatenates two string
 *@dest: string to append to
 *@src: string to added
 *Return: returns dest
 */

char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
