#include "main.h"
#include <string.h>

/**
 *_strcmp - compares two strings
 *@s1: First string
 *@s2: Second string to added
 *Return: returns res, true or false
 */

int _strcmp(char *s1, char *s2)
{
int res = strcmp(s1, s2);
return (res);
}
